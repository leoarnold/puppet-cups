HOSTS:
  fedora-21-docker:
    platform: fedora-21-docker
    hypervisor: docker
    image: fedora:21
    docker_preserve_image: true
    docker_cmd: '["/sbin/init"]'
CONFIG:
  trace_limit: 200
  log_level: debug
