HOSTS:
  debian-8-docker:
    platform: debian-8-docker
    hypervisor: docker
    image: debian:8
    docker_preserve_image: true
    docker_image_commands:
    - 'apt-get install -y wget'
CONFIG:
  trace_limit: 200
  log_level: debug
