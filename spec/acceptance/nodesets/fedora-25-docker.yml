HOSTS:
  fedora-25-docker:
    platform: fedora-25-docker
    hypervisor: docker
    image: fedora:25
    docker_preserve_image: true
    docker_cmd: '["/sbin/init"]'
CONFIG:
  trace_limit: 200
  log_level: debug
