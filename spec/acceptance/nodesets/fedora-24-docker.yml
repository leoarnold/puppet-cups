HOSTS:
  fedora-24-docker:
    platform: fedora-24-docker
    hypervisor: docker
    image: fedora:24
    docker_preserve_image: true
    docker_cmd: '["/sbin/init"]'
CONFIG:
  trace_limit: 200
  log_level: debug
