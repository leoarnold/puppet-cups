---
engines:
  duplication:
    enabled: true
    config:
      languages:
      - ruby
    exclude_paths:
    - spec/
  fixme:
    enabled: true
  rubocop:
    enabled: true
    checks:
      Rubocop/Style/PercentLiteralDelimiters:
        enabled: false
ratings:
  paths:
  - "lib/**/*.rb"
